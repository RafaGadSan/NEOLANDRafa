const sentence = "Yo soy batman";

let index = 5;

console.log(`En el index ${index}, esta el elemento ${sentence.at(index)}`);

console.log(`En el index ${index}, esta el elemento ${sentence.at(index - 1)}`);
